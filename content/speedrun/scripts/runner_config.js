/**
 * 네모세모동그라미 — 수치 상수
 *
 * 팔레트 인덱스 (palette_runner.json 기준):
 *   0  투명     1  종이(밝음)  2  종이(어두움)  3  지면(모래)
 *   4  잉크(검정) 5  초록(기본)  6  진초록       7  금색(코인)
 *   8  주황(코인강조) 9  밝은초록  10 cyan(부스트)  11 빨강(느림)
 */
export const CFG = {
    // ── 월드 ──────────────────────────────────────────────────────
    GAME_W:      480,
    GAME_H:      270,
    GROUND_Y:    220,   // 지면 Y (픽셀, 캐릭터 바닥 기준)
    CHAR_LEFT:   60,    // 캐릭터 고정 X
    CHAR_SIZE:   24,    // 캐릭터 기본 크기 (정사각형 한 변, px) - 단이(안티그래비티) 24px로 업그레이드!

    // ── 물리 ──────────────────────────────────────────────────────
    GRAVITY:       900,
    JUMP_IMPULSE:  340,
    DELTA_CAP:     0.05,
    HIT_INSET:     3,      // 24px 크기에 맞추어 히트인셋 보정

    // ── 스크롤 / 속도 ─────────────────────────────────────────────
    SCROLL_BASE:        70,   // 기본 스크롤 속도 (px/s)
    SCROLL_SCORE_CHUNK: 100,   // 점수 몇 점당 속도 증가
    SCROLL_STEP:         10,   // 속도 증가량
    SPEED_SLOW:          0.5,  // 세모 변신 시 배율
    SPEED_BOOST:         2.0,  // 동그라미 변신 시 배율

    // ── 타이머 시스템 ─────────────────────────────────────────────
    TIMER_START:        30.0,  // 시작 시간 (초)
    TIMER_CAP:         180.0,  // 최대 충전 시간
    COIN_TIME_ADD:      10.0,  // 코인 수집 시 추가 시간
    TIMER_WARN_30:      30.0,  // 경고 1단계 (타이머 바 빨강)
    TIMER_WARN_10:      10.0,  // 경고 2단계 (red_tint 강화)
    TIMER_WARN_5:        5.0,  // 경고 3단계 (flash + 숫자 오버레이)

    // ── 변신 시스템 ───────────────────────────────────────────────
    TRANSFORM_DURATION:  5.0,  // 변신 지속 시간 (초)
    TRANSFORM_BLINK_SEC: 1.0,  // 복귀 전 깜빡임 시작 시점 (초 남았을 때)
    TRANSFORM_BLINK_HZ:  6.0,  // 깜빡임 주파수

    // ── 하이브리드 스테이지 시스템 ───────────────────────────────
    STAGE_RUN_TIME:      15.0, // 스테이지 전환까지 러너 달리는 시간 (초)
    STAGE_TRANSFORM_DUR:  3.0, // 스테이지 내 변신 카드 효과 유지 시간 (초)
    DRAG_SMOOTH:         0.20, // 드래그 마우스 추적 부드러움 계수 (Lerp)

    // ── 아이템 스폰 ───────────────────────────────────────────────
    ITEM_CD_MIN:     0.5,   // 아이템 스폰 최소 쿨다운
    ITEM_CD_MAX:     1.4,   // 아이템 스폰 최대 쿨다운
    COIN_PROB:       0.70,  // 코인 스폰 확률
    ITEM_FAST_PROB:  0.15,  // 빠름(동그라미) 아이템 확률
    ITEM_SLOW_PROB:  0.15,  // 느림(세모) 아이템 확률

    ITEM_W:          8,    // 아이템 폭 (px - 코인용)
    ITEM_H:          8,    // 아이템 높이 (px - 코인용)
    ITEM_TRANSFORM_SIZE: 12, // 변신 아이템 크기 (파랑 동그라미 / 빨간 세모용)
    ITEM_BOTTOM_OFF: 6,    // 지면 위 기본 오프셋
    ITEM_AIR_PROB:   0.4,  // 공중 스폰 확률
    ITEM_AIR_MIN:    32,   // 공중 스폰 최소 높이
    ITEM_AIR_MAX:    68,   // 공중 스폰 최대 높이

    // 코인 체인
    CHAIN_LEN_MIN:  3,
    CHAIN_LEN_MAX:  6,
    CHAIN_GAP:      14,

    // ── 지형 ──────────────────────────────────────────────────────
    TERRAIN_FLAT_LEN_MIN:   120,   // 평지 최소 길이 (px)
    TERRAIN_FLAT_LEN_MAX:   280,   // 평지 최대 길이
    TERRAIN_SLOPE_LEN_MIN:   80,   // 경사 최소 길이
    TERRAIN_SLOPE_LEN_MAX:  180,   // 경사 최대 길이
    TERRAIN_MAX_DY:          30,   // 오르막/내리막 최대 높이차 (px)
    TERRAIN_LERP:            0.04, // 지면 Y Lerp 속도 (캐릭터 추적)

    // ── 점수 ──────────────────────────────────────────────────────
    SCORE_PER_SEC:  6,

    // ── 팔레트 인덱스 ─────────────────────────────────────────────
    PAL: {
        TRANSPARENT:  0,
        PAPER:        1,
        PAPER_DK:     2,
        GROUND:       3,
        INK:          4,
        CHAR_BASE:    5,   // 네모 기본색
        CHAR_DK:      6,   // 그림자
        COIN:         7,   // 코인 노랑
        COIN_ACC:     8,   // 코인 강조
        CHAR_BOOST:  10,   // 동그라미 cyan
        CHAR_SLOW:   11,   // 세모 빨강
    },

    // ── 충돌 ──────────────────────────────────────────────────────
    COLLISION_IGNORE_MS: 1000,  // 게임 시작 직후 충돌 무시

    // ── 게임오버 ──────────────────────────────────────────────────
    GAMEOVER_DT_SLOW:    0.3,   // 슬로우모션 dt 배율
    GAMEOVER_ZOOM_DUR:   0.3,   // 줌인 시간 (초)

    // ── 레거시 (RunnerHud 호환) ───────────────────────────────────
    TIMER_CYCLE_SEC:    60,     // HUD 타이머 바 주기 (기존 스타일)
    STARTER_SCORE_PER_COIN: 100,
};
